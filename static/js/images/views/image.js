/* */
"use strict";

define([
    'backbone'
    ], function(Backbone) {

        var ImageView = Backbone.View.extend({

            initialize: function() {
            },

            render: function() {
            }

        });

        return ImageView;
    }
);
